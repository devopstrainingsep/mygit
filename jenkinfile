pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'build is running'
             }
          }
	stage('test') {
            steps {
                echo 'test part is running'
            }
        }
    }
}
